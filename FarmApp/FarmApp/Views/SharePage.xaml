<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="FarmApp.Views.SharePage"
             Title="{Binding Title}"
             BackgroundColor="LightGray">
    <StackLayout VerticalOptions="CenterAndExpand" HorizontalOptions="Center">
        <Frame IsClippedToBounds="True" Padding="0" CornerRadius="200" >
            <Button Text="Open External map" BackgroundColor="White" CornerRadius="150"  Command="{Binding CopyCommand}"/>
        </Frame>
        <Frame IsClippedToBounds="True" Padding="0" CornerRadius="200" >
            <Button Text="Copy Link" BackgroundColor="White" CornerRadius="150"  Command="{Binding CopyCommand}"/>
        </Frame>
        <Label Text="{Binding Link}"/>
    </StackLayout>
  
</ContentPage>